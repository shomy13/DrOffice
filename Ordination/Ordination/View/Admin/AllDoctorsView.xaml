<UserControl x:Class="Ordination.View.Admin.AllDoctorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             >
    <UserControl.Resources>
        <ResourceDictionary Source="AdminViewResources.xaml"/>
    </UserControl.Resources>
    <DockPanel>
        <ListView DockPanel.Dock="Top"
                  AlternationCount="2"
                  ItemContainerStyle="{StaticResource ItemStyle}"
                  ItemsSource="{Binding}"
                  >
          
            <ListView.View>
                <GridView >
                    <GridViewColumn Header="First Name" />
                    <GridViewColumn Header="Last Name" />
                    <GridViewColumn Header="Address" />
                    <GridViewColumn Header="Email" />
                    <GridViewColumn Header="Phone Number" />
                    <GridViewColumn Header="Birth date" />
                    <GridViewColumn Header="User name" />
                </GridView>
                
            </ListView.View>
        </ListView>
        <StackPanel HorizontalAlignment="Left">
            <Button Content="Delete" Width="100" Style="{StaticResource ButtonStyle}" Command="{Binding DeleteDoctorUC}"/>
        </StackPanel>
    </DockPanel>
    
    
</UserControl>
